






fn main()
{
    let mut x : i32  = 0;
    x = 7;
    
    let x = x; // x is immutable now and is bound to 7
   
    let y = 4;
    let y = "I can also be bound to text!"; // y is of a different type now
}
